# ML Models

This folder will host ML model code, embeddings, and training scripts.

Planned structure:
- `embeddings/` — sentence-transformers models
- `parsers/` — resume parsers using spaCy/LayoutLM
- `scoring/` — ATS scoring and heuristics
