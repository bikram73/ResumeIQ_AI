# Backend (FastAPI)

This folder contains the FastAPI backend skeleton. Key files:
- `app/main.py` — starter API endpoints for upload, analyze, and results.

The provided synopsis PDF is included at the project root as `synopsis_1764500301.pdf` and is referenced in docs.
